//noPage

class Ecrypt {
  constructor() {
    console.warn("lib/crypt NOT WORKING ON WEB")
  }

  encode(text) {
 
  }

  decode(text) {
   
  }

  sha5(string, toggle, use_sha = true) {
   
  }
  getIV(length) {
  
  }

  rand(min, max) {
 
  }

  ord(string) {
   
  }
}
export default Ecrypt